{% extends "base.html.twig" %}

{% block body %}
	<div class="bg-black d-flex flex-column justify-content-between">
		{% if error %}
			<div class="alert alert-danger">{{error.messageKey}}</div>
		{% endif %}
		<form method="post" class="container text-secondary">
			<label for="username">Nom d'utilisateur</label>
			<input type="text" id="username" name="_username" value="{{last_username}}" class="form-control" required/>
			<label for="password">Mot de passe</label>
			<input type="password" id="password" name="_password" class="form-control" required/>
			<input type="hidden" name"_target_path" value="/">
			<button type="submit" class="btn btn-outline-light mt-3">Connexion</button>
		</form>
	</div>
{% endblock %}
